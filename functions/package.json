{
  "name": "mobile-project-functions",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "lint": "eslint .",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "build": "echo Using Node.js functions"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.0.0"
  }
}
